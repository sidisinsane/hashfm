{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/sidisinsane/hashfm/main/schema/hashfm-config.schema.json",
  "$comment": "Schema version: 1.1.0 | Last updated: 2026-05-08",
  "title": "hashfm Configuration",
  "description": "The shared .hashfm config file used by hashfm-compatible tools. Each tool owns a top-level namespace following the 'hashfm-*' naming convention.",
  "type": "object",
  "properties": {
    "version": {
      "type": "string",
      "description": "The version of the hashfm convention used in this project."
    },
    "project": {
      "type": "object",
      "description": "Project metadata.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the project."
        },
        "description": {
          "type": "string",
          "description": "A brief description of the project."
        }
      },
      "required": ["name"],
      "additionalProperties": true
    }
  },
  "patternProperties": {
    "^hashfm-[a-z][a-z0-9-]*$": {
      "type": "object",
      "description": "Tool-specific configuration. Each hashfm-compatible tool owns its own namespace."
    }
  },
  "additionalProperties": true
}
